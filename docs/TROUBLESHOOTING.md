# Troubleshooting

Q: It does not work.
A: Have you tried turning it off and on again?

Q: It crashed.
A: That is a feature. It is the "safety off" mode.

Q: Where are the logs?
A: They log to /dev/null by default. For a reason.
