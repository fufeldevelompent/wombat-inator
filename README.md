# wombat-inator

Task manager that clones wombats for every finished task. Each task you close
adds one (1) pixel-art wombat to your `wombats/` folder.

## Run
```
./wombat-inator.sh add "write quarterly report"
./wombat-inator.sh done 1
ls wombats/
```
